export default {
	// Global override for the default path to where repositories will be stored
	// Note that in repository mirror a new folder with the name of the user
	// is created, and the repositories are then cloned/pulled inside it.
	repositoriesPath: "~/repositories"
}
